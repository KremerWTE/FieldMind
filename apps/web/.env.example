# Copy to .env.local for local development
# In production, set NEXT_PUBLIC_API_URL in your hosting provider's environment settings

# URL of the FieldMind API (no trailing slash)
# Defaults to http://localhost:3001 if not set
NEXT_PUBLIC_API_URL=http://localhost:3001
